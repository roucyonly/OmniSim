# OmniSim - 造物主模拟器 架构设计

## Context

用户想构建一个"造物主"模拟游戏：玩家创建角色（天道之子）和世界，AI Agent 在沙盒中自主交互演化。玩家只能通过天道之子间接影响世界，核心看点是 AI Agent 之间的涌现行为。参考斯坦福 AI 小镇的生成式 Agent 架构，结合武侠（笑傲江湖）等世界观模板。

---

## 一、系统架构总览

```
┌─────────────────────────────────────────────────────┐
│                   Web Frontend (React)               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │
│  │ 世界编辑器 │ │ 沙盒观景台 │ │ 天道面板  │ │ 叙事流  │ │
│  └──────────┘ └──────────┘ └──────────┘ └────────┘ │
└──────────────────────┬──────────────────────────────┘
                       │ WebSocket + REST
┌──────────────────────┴──────────────────────────────┐
│                API Gateway (FastAPI)                  │
└──────────┬──────────┬──────────┬────────────────────┘
           │          │          │
┌──────────▼──┐ ┌─────▼─────┐ ┌▼──────────────────┐
│ 模拟引擎     │ │ 规则引擎   │ │ LLM Gateway       │
│ (Simulation) │ │ (Rules)   │ │ (Prompt/Cost管理)  │
├─────────────┤ ├───────────┤ └───────────────────┘
│ Agent System│ │ 战斗系统   │
│ Memory      │ │ 功法/技能  │        ┌───────────┐
│ Reflection  │ │ 移动/寻路  │◄──────►│ LLM API   │
│ Planning    │ │ 资源/经济  │        │(Claude/GPT)│
│ Social      │ │ 天气/自然  │        └───────────┘
├─────────────┤ └───────────┘
│ Event System│
│ World State │
│ Time System │
└──────┬──────┘
       │
┌──────▼──────────────────────────────────────────────┐
│              数据层 (PostgreSQL + Redis)              │
│  PostgreSQL: 世界状态/Agent记忆/历史事件/用户数据      │
│  Redis:      模拟缓存/实时状态/消息队列               │
└─────────────────────────────────────────────────────┘
```

---

## 二、核心模块设计

### 1. 世界引擎 (World Engine)

负责整个虚拟世界的状态管理和时间推进。

- **MapSystem**: 世界地图为 N×N 方格网格，每个格子 = 1 单位长度
  - 每个格子有属性：地形类型（平原、山地、河流、森林...）、归属（城市/门派领地/野外）、建筑信息
  - 地点类型层级：**城市**（粗线方块，占据多个格子）→ **建筑**（方块，如客栈、铁匠铺，占 1-2 格）→ **野外**（无建筑覆盖的格子）
  - 格子之间可直接相邻移动，无需预定义路径
- **MovementSystem（行进系统）**: Agent 在格子间移动，由规则引擎驱动
  - 每个 tick Agent 可移动若干格，移动距离 = 基础速度 × 轻功加成 × 地形系数 × 天气系数
  - 地形系数：平原 1.0，山路 0.5，河流 0.3（需渡船），森林 0.7
  - Agent 在行进中是"在路上"状态，每 tick 更新格子位置（在地图上可见地移动）
  - 示例：普通人每 tick 走 2 格，轻功高手每 tick 走 5 格
- **TimeSystem**: Tick 驱动的时间系统。1 tick = 游戏内 1 时辰（~2小时）。每天 12 tick，提供加速/暂停
- **WeatherSystem**: 天气/自然演化。造物主可通过天道之子影响局部天气
- **ResourceSystem**: 物资、金钱、药材等资源管理
- **FactionSystem**: 门派/势力关系网，动态结盟/敌对

### 2. Agent 系统 (斯坦福 AI 小镇 核心架构)

每个 NPC 和天道之子都是一个 Agent，包含：

- **MemoryStream（记忆流）**: 按时间序列记录所有感知和经历。每条记忆有：时间戳、描述、重要性分数（1-10）。检索时按 **相关性 × 重要性 × 时效性** 排序
- **Reflection（反思）**: 每隔 N tick，Agent 总结近期记忆，提炼出高层认知（"岳不群不可信"、"我对剑法有天赋"）。用 LLM 生成
- **Planning（规划）**: 每天生成日程计划。LLM 基于记忆和反思生成 → 规则引擎分解为可执行动作。遇到突发事件可重规划
- **SocialGraph**: 关系网络。记录与其他 Agent 的关系（师徒、敌对、恋人、同门）。关系影响交互权重

**分层 Agent 系统（让所有角色都"活着"）**:

| 层级 | 代表角色 | 决策方式 | 记忆 | LLM 成本 |
|------|---------|---------|------|---------|
| **T1 全 Agent** | 天道之子、令狐冲、任盈盈、岳不群等关键角色 | LLM 驱动规划 + 规则引擎执行 | 完整记忆流 + 反思 | 高 |
| **T2 活跃 Agent** | 掌柜、镖师、门派弟子 | 行为树骨架 + LLM 点缀（仅对话时调用） | 压缩记忆（只保留关键事件） | 中 |
| **T3 环境 Agent** | 农夫、路人、小兵 | 纯行为树/状态机，不调 LLM | 短期记忆（近几个 tick） | 极低 |

**动态晋升机制**：T3 的农夫平时按行为树过日子（种地、吃饭、睡觉），但当他被卷入 T1 的事件（比如令狐冲在他田里打架），他就**临时晋升为 T2**——获得这段经历的完整记忆，后续行为会被这段经历影响。如果他反复参与重要事件，可以**永久晋升为 T2 甚至 T1**。这样所有 NPC 都是"活的"，只是活的深度不同，成本也可控。

**决策路由（关键 - 控制成本）**:

| 决策类型 | 处理方 | 示例 |
|---------|--------|------|
| 复杂对话/叙事 | LLM | NPC 之间深度对话、情感表达 |
| 日程规划 | LLM | 每日计划生成 |
| 反思总结 | LLM | 定期认知提炼 |
| 移动/行进 | 规则引擎 | 路径寻路、行进速度计算（距离/地形/轻功/天气） |
| 战斗判定 | 规则引擎 | 伤害计算、胜负判定 |
| 资源获取 | 规则引擎 | 买药、吃饭、修炼 |
| 简单日常 | 规则引擎 | 睡觉、吃饭、巡逻 |

### 3. 世界初始化流程

玩家进入游戏后的启动顺序：
1. **选择/创建世界模板**：选择预设模板（笑傲江湖等）或让 AI 生成自定义世界
2. **生成世界地图**：AI 根据模板生成 N×N 方格地图，铺设地形（野外不同地形不同颜色）、城市（地板是棕红色）、建筑
3. **生成人物列表**：AI 根据模板生成所有角色（T1 关键角色 + T2/T3 批量生成），每个角色有初始属性、位置。天道之子金色，重要角色粉红色，T2角色绿色，T3灰色
4. **造物主编辑**：玩家可查看并修改人物属性（预留编辑接口，后续开放完整编辑器）
5. **创建天道之子**：玩家创建自己的角色（名字、性格、天赋、道具、tag/状态、初始位置）
6. **启动模拟**：世界开始运行，tick 循环启动

### 4. 模拟循环 (Simulation Loop)

```
每个 Tick:
  1. TimeSystem 推进时间
  2. WeatherSystem 更新天气
  3. 对每个 Agent:
     a. 感知阶段：收集周围环境信息（附近的人、地点状态、事件）
     b. 决策阶段：
        - 检查当前计划，是否需要执行
        - 规则引擎判断：日常动作直接执行
        - 复杂决策 → LLM（带上下文缓存）
     c. 执行阶段：规则引擎执行动作、更新世界状态
     d. 记忆写入：将本 tick 经历写入记忆流
  4. 每 N tick：触发反思（LLM 批量处理）
  5. EventSystem 检查触发条件
  6. 通过 WebSocket 推送状态变化到前端
  7. 每天（每 12 tick）结束时：
     - 汇总本日所有事件，筛选出重大事件和重要角色的强烈情绪波动
     - 如果有值得记录的内容 → LLM 生成一篇"每日纪事"（短文形式）
     - 如果全是日常 → 跳过，不生成
     - 纪事存入世界历史，前端可在日志流中查看
```

### 5. 造物主系统 (Creator System)

造物主的**唯一**影响渠道——通过天道之子：

- **传音**: 向天道之子发送心灵信息（LLM 以"天启"形式融入天道之子的记忆流）
- **顿悟**: 给天道之子一个技能突破/领悟契机
- **天象**: 改变天道之子附近的天气（暴雨、大雾、晴天）
- **缘分**: 在天道之子周围安排"巧合"（偶遇某人、发现某物）——微调 Agent 调度权重

造物主**不能**：
- 直接控制天道之子的行动
- 直接与其他 NPC 交互
- 直接修改世界状态（除了天道之子周围的天象）

### 6. 世界模板系统 (Theme System)

可插拔的世界观模板，每个模板定义：

```yaml
# 笑傲江湖模板示例
theme:
  name: "笑傲江湖"
  locations:
    - 华山派: { type: 门派, terrain: 山岳 }
    - 洛阳: { type: 城市, terrain: 平原 }
    - 梅庄: { type: 庄园, terrain: 湖畔 }
  elements:
    - 内功体系: [内力等级, 经脉, 丹田]
    - 剑法体系: [独孤九剑, 岳家剑法, 辟邪剑法]
  factions:
    - 五岳剑派: { allies: [泰山, 衡山, 嵩山, 恒山], enemy: 日月神教 }
  events_timeline:
    - { trigger: day_30, event: "刘正风金盆洗手大会" }
  templates:
    - 令狐冲: { personality: "洒脱不羁", talent: "剑法天才" }
```

---

## 三、技术栈

| 层 | 技术 | 理由 |
|----|------|------|
| 前端 | React + TypeScript + Vite | 快速迭代，生态丰富 |
| UI 框架 | Tailwind CSS + shadcn/ui | 美观、可定制 |
| 状态管理 | Zustand | 轻量，适合实时更新 |
| 地图可视化 | Canvas 2D | 地点分布、Agent 实时位置、行进动画 |
| 后端 | Python + FastAPI | AI 生态最好，异步支持好 |
| 实时通信 | FastAPI WebSocket | 推送模拟状态 |
| 数据库 | PostgreSQL | 关系型数据，Agent 记忆查询 |
| 缓存 | Redis | 模拟 tick 缓存，消息队列 |
| LLM | Claude API (主) + GPT (备) | 叙事质量好，支持 prompt caching |
| 任务队列 | Celery + Redis | LLM 调用批处理，异步任务 |

---

## 四、前端交互设计

### 主视图：全局世界地图（Canvas 2D）

- 默认展示完整世界地图，造物主的"上帝视角"
- **视觉规范**：
  - 人物 = 圆点（T1 大且亮，T2 中等，T3 小且暗）
  - 建筑 = 方块（带图标/文字标注）
  - 城市 = 粗线方块（内含多个建筑方块）
  - 路径 = 细线连接地点
- **行进可视化**：Agent 在地点间移动时，圆点沿路径平滑移动，速度对应行进速度
- **天气/环境**：地点周围有天气效果（雨滴、雾气等视觉反馈）

### 左下角：实时动态日志

- 滚动的消息流，实时展示 T1 全 Agent 和附近 T2 的关键行为
- 每条日志格式：`[时辰] 角色名 - 行为描述`（如 `「午时」令狐冲 - 在华山练剑，偶遇岳灵珊`）
- **点击日志条目**：地图自动缩放并聚焦到该角色当前位置，同时弹出角色详情面板
- 日志按重要性强调显示（重大事件高亮）

### 交互操作

- **缩放/平移**：鼠标滚轮缩放地图，拖拽平移
- **点击人物**：弹出角色详情（状态、记忆摘要、当前计划）
- **点击地点**：显示地点信息（当前在场人物、资源、天气）
- **天道面板**：右侧固定面板，提供传音/顿悟/天象/缘分操作按钮

---

## 六、数据模型（核心实体）

```
World ──< Location ──< LocationConnection >
  │
  ├──< Agent (NPC + 天道之子)
  │      ├──< Memory[]
  │      ├──< Reflection[]
  │      ├──< DailyPlan
  │      ├──< Skill[]
  │      └──< Relationship[]
  │
  ├──< Faction ──< FactionRelation >
  │
  ├──< Event[] (世界事件)
  │
  └──< WorldConfig (模板配置)
```

---

## 七、项目目录结构

```
OmniSim/
├── client/                    # React 前端
│   ├── src/
│   │   ├── components/
│   │   │   ├── world/         # 世界编辑器组件
│   │   │   ├── sandbox/       # 沙盒观景台组件
│   │   │   ├── creator/       # 造物主面板（传音、顿悟等）
│   │   │   └── narrative/     # 叙事/日志流组件
│   │   ├── stores/            # Zustand 状态
│   │   └── api/               # WebSocket/REST 客户端
│   └── package.json
│
├── server/                    # Python 后端
│   ├── app/
│   │   ├── main.py            # FastAPI 入口
│   │   ├── api/               # REST + WebSocket 路由
│   │   ├── simulation/        # 模拟引擎核心
│   │   │   ├── engine.py      # 模拟主循环
│   │   │   ├── time_system.py
│   │   │   ├── weather.py
│   │   │   └── event_system.py
│   │   ├── agents/            # Agent 系统
│   │   │   ├── agent.py       # Agent 基类
│   │   │   ├── memory.py      # 记忆流
│   │   │   ├── reflection.py  # 反思
│   │   │   ├── planner.py     # 规划器
│   │   │   └── social.py      # 社交关系
│   │   ├── rules/             # 规则引擎
│   │   │   ├── combat.py      # 战斗
│   │   │   ├── movement.py    # 移动
│   │   │   ├── skills.py      # 技能/功法
│   │   │   └── economy.py     # 经济
│   │   ├── llm/               # LLM 集成
│   │   │   ├── gateway.py     # LLM 调用网关
│   │   │   ├── prompts.py     # Prompt 模板
│   │   │   └── cache.py       # 响应缓存
│   │   ├── creator/           # 造物主系统
│   │   │   ├── influence.py   # 传音/顿悟/天象
│   │   │   └── coincidence.py # 缘分/巧合
│   │   ├── world/             # 世界管理
│   │   │   ├── map.py         # 地图/地点
│   │   │   ├── factions.py    # 势力
│   │   │   └── resources.py   # 资源
│   │   ├── themes/            # 世界模板
│   │   │   ├── base.py        # 模板基类
│   │   │   └── xiaoao/        # 笑傲江湖模板
│   │   │       ├── config.yaml
│   │   │       ├── locations.py
│   │   │       └── characters.py
│   │   └── models/            # 数据库模型 (SQLAlchemy)
│   └── tests/
│
├── shared/                    # 前后端共享类型定义
└── docker-compose.yml         # 开发环境一键启动
```

---

## 八、MVP 分阶段路线

### Phase 1: 核心骨架（2-3 周）
- 搭建 FastAPI + React 项目结构
- 实现最简模拟循环（tick 推进、Agent 感知-决策-执行）
- 实现基础记忆流（写入 + 检索）
- 一个固定地点、2-3 个 NPC，观察他们的一天
- 前端：简单的文字日志流，展示 Agent 行为

### Phase 2: 世界与 Agent（3-4 周）
- 地图系统（多地点、移动）
- Agent 规划系统（每日日程 + LLM 生成）
- 反思系统（定期总结）
- 社交系统（对话、关系变化）
- 规则引擎：战斗、功法
- 前端：地图可视化 + Agent 详情面板

### Phase 3: 造物主玩法（2-3 周）
- 世界编辑器（创建/修改世界）
- 天道之子创建界面
- 造物主影响机制（传音、顿悟、天象）
- 笑傲江湖模板完整实现

### Phase 4: 涌现与叙事（2-3 周）
- 势力系统（门派关系动态变化）
- 重大事件触发与传播
- 叙事流（以故事形式呈现世界变化）
- LLM 成本优化（缓存、批处理、分级调用）

---

## 九、验证方案

1. **Phase 1 验证**: 3 个 NPC 在同一地点生活一天，能自主吃饭、休息、聊天
2. **Phase 2 验证**: 笑傲江湖世界运行，NPC 之间产生关系变化，能看到令狐冲和岳不群的师徒互动
3. **Phase 3 验证**: 创建天道之子，通过传音影响其决策，观察间接影响世界的效果
4. **Phase 4 验证**: 世界运行 30 天游戏时间，涌现出门派冲突、人物恩怨等非线性叙事
